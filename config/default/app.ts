/**
 * 应用级默认配置
 */
import { AppConfig } from "../app.config";

export const defaultAppConfig: AppConfig["app"] = {
  defaultMode: "Barcode",
  alignSubmenus: true,
  backgroundColor: "#0b0b0f",
  wakeLock: true,
};
